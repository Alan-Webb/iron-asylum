const Hero = () => {
	return (
		<div>
			{/* Banner Section */}
			<div
				id="home"
				className="relative h-screen bg-black overflow-hidden flex flex-col lg:flex-row items-center justify-center lg:justify-between"></div>
		</div>
	);
};

export default Hero;
